
const config = {

    api: {
        baseUrl: "https://localhost:3001",
        useAuth: true,
        useMock: false
    },
    assets: {
        sameAsBaseUrl: true,
        directory: "",
        src: null,
        defaultImage: null,
        defaultDocument: null,
        defaultThumbnail: null,
    },
    isMockMode: true,
}

export default config;